<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lai.test.app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.lai.test.app.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <receiver
                android:enabled="true" android:label="SDCardMountReceiver"
                android:exported="true"
                android:name="com.lai.test.broadcasts.SDCardBroadcastReceiver">
                <intent-filter>
                    <action
                        android:name="android.content.Intent.ACTION_MEDIA_MOUNTED">

                    </action>

                </intent-filter>
             </receiver>
        </activity>
    </application>

</manifest>
